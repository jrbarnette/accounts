#!/bin/bash

ant tests || exit $?
echo
export CLASSPATH=test-classes:accounts.jar:$CLASSPATH
TESTS=( $(sed -e 's=tests/==' -e 's=\.java$==' -e 's=/=.=g' < tests-list) )
for t in "${TESTS[@]}"
do
    echo $t
    java org.junit.runner.JUnitCore $t
done
